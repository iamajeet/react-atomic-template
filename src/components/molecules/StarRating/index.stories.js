import React from 'react';
import { storiesOf } from '@storybook/react';
import { StarRating } from '../../../components';

storiesOf('StarRating', module).addWithJSX('default', () => <StarRating />);
